{
  "name": "{{package-name.paramCase()}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": ">=18.0.0 <19"
  },
  "scripts": {
    "setup": "firebase setup:emulators:firestore",
    "deploy": "firebase deploy --only firestore:rules",
    "test": "firebase emulators:exec --only firestore \"mocha firestore.rules.spec.ts\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "1.3.16",
    "@types/mocha": "^8.2.2",
    "chai": "^4.3.4",
    "mocha": "^9.2.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "mocha": {
    "recursive": true,
    "reporter": "nyan",
    "require": "ts-node/register",
    "watch-extensions": "ts"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true
  }
}